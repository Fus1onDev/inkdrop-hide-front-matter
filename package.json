{
  "name": "hide-front-matter",
  "main": "./lib/index",
  "version": "0.1.2",
  "description": "Hide front-matter from note lists",
  "keywords": [],
  "repository": "https://github.com/fus1ondev/inkdrop-hide-front-matter",
  "license": "MIT",
  "engines": {
    "inkdrop": "^5.x"
  },
  "scripts": {
    "build": "babel src/ -d lib/ -s false",
    "dev": "babel src/ -s -d lib/ --watch",
    "lint": "npx prettier --write 'src/**/*.js' && eslint --fix --ext .js src"
  },
  "dependencies": {
    "remove-markdown": "^0.5.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.10",
    "@babel/core": "^7.22.11",
    "@babel/eslint-parser": "^7.22.11",
    "@babel/preset-env": "^7.22.14",
    "@babel/preset-react": "^7.22.5",
    "eslint": "^8.48.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-react": "^7.33.2",
    "prettier": "^3.0.3"
  }
}
